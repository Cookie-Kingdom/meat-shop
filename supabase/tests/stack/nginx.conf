# Card ^ref-72 — one URL for supabase-js: /auth/v1 -> GoTrue, /rest/v1 -> PostgREST, each with
# its prefix stripped, as Supabase's own gateway does.
server {
  listen 80;

  location /auth/v1/ {
    proxy_pass http://auth:9999/;
  }

  location /rest/v1/ {
    proxy_pass http://rest:3000/;
  }
}
